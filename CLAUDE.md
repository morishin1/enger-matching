@AGENTS.md

## UI規約（メモリ）

- **アイコンは Google Material Symbols Outlined に統一**（`material-symbols-outlined`）。
  - フォントは `app/layout.tsx` で読込済み。新規追加は不要。
  - SVG アイコンの中央レジストリは `src/components/icons.tsx`（`Icons.<name>` を呼び出す）。新規アイコンはここに追加してから使う。
  - 旧 `material-icons-outlined`（Material Icons）は使用しない。混在するとフォント未読込でグリフが表示されない事故になる。
- ブランド表記：dx（企業/社内向け）は「ENGER business」（ロゴ＋business）。
- 企業に見せる人材情報は常に匿名（イニシャル＋スキル＋単価。氏名/連絡先は担当が仲介）。

## 開発運用（メモリ）

- **タスク開始時にモデル推奨を1行提示する**：各タスク（チケット）の最初の応答の冒頭で、
  「💡 推奨モデル: Sonnet（表記変更のみの軽作業のため）」の形式で適したモデルを提示する。
  - 目安：表記変更・小さなUI修正・単純なバグ修正・検索条件の追加 → **Sonnet** ／
    新機能・DB設計やセキュリティ判断を伴うもの・複数画面にまたがる大きな変更 → **Opus**。
  - 現在のモデルが推奨と異なる場合は「/model で切替できます」と一言添える
    （切替はユーザーの判断に任せ、作業はそのまま続行する）。

## クライアント/サーバー境界（メモリ・事故防止）

- **`"use client"` のファイルからは「コンポーネント（大文字始まり）」だけを export する。**
  判定・整形などの純粋関数は `src/lib/` に置く。
  - 理由：`"use client"` のモジュールの関数をサーバーコンポーネントから呼ぶと
    `Attempted to call X() from the server but X is on the client.` で**その画面が丸ごと 500 になる**。
    型検査もビルドも通ってしまい、本番でその画面を開いて初めて落ちる（2026-07 のマッチング画面全面停止の原因）。
  - ブラウザ専用でサーバーから呼びようがないもの（`toast()` 等）は、直前に `@client-only` と書けば検査から除外される。
  - CI の `Client/Server boundary check`（`node scripts/check-client-exports.mjs`）で機械的に検出する。
- 動的レンダリングの画面はビルド時に描画されないため、**ビルド成功＝画面が開ける保証にならない**。
  大きめの変更を入れたら、対象画面を実データで1回開いて確認する。
